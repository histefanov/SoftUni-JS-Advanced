function stringLength(firstParam, secondParm, thirdParam) {
    const sumLength = firstParam.length + secondParm.length + thirdParam.length;
    const averageLength = Math.floor(sumLength / 3);
    console.log(sumLength);
    console.log(averageLength);
}